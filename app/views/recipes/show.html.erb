
<div class=" ms-3">
<p style="color: green"><%= notice %></p>

<%= render @recipe %>

  <div class="my-2">
    <%= link_to "Éditer cette recette", edit_recipe_path(@recipe), class: "btn btn-primary" %>
    <%= link_to "Retour aux recettes", recipes_path, class: "btn btn-secondary" %>
  </div>

  <div class="my-1 mb-5">
    <%= button_to "Supprimer cette recette", @recipe, method: :delete, class: "btn btn-danger"%>
  </div>
</div>
